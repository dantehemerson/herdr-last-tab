[package]
name = "herdr-last-workspace"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Herdr plugin action that toggles to the previously focused workspace."

[dependencies]
fs2 = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
